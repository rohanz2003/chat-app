# MongoDB Connection String
# For local MongoDB (default):
MONGO_URI=mongodb://localhost:27017/chatapp

# Server Port
PORT=5000

# Admin Email for OTP-based Login
# This is the email address that will receive the OTP to access the admin panel.
# IMPORTANT: Do NOT commit your actual email password/app password to Git.
ADMIN_EMAIL=zenderohan2012@gmail.com

# JWT Secret for Admin Session Tokens
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

EMAIL_USER=your.zenderohan2012@gmail.com
# Plain password (not recommended) or App Password
EMAIL_PASSWORD=
# Alternate name used in code for Gmail app password
GMAIL_APP_PASSWORD=rqwmpsxigahtoocg
