# MongoDB Connection String
# For local MongoDB (default):
MONGO_URI=mongodb://localhost:27017/chatapp

# Server Port
PORT=5000

# Email (SMTP) settings for sending feedback notifications
# Example: use a Gmail account with an App Password (recommended)
# Set either `EMAIL_PASSWORD` or `GMAIL_APP_PASSWORD` to an app password created in your Google Account.
# Do NOT commit real credentials. Copy this file to `.env` and fill values there.
EMAIL_USER=your.zenderohan2012@gmail.com
# Plain password (not recommended) or App Password
EMAIL_PASSWORD=
# Alternate name used in code for Gmail app password
GMAIL_APP_PASSWORD=rqwmpsxigahtoocg
